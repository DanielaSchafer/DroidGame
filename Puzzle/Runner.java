public class Runner
{
  ArrayList<Tool> tools = new ArrayList<String>();
//array to hold the commands
//change positon of the droid based on the commands

public parseTools(Droid x, ArrayList<Tool> tools)
{
  for(int i = 0; i<tools.length; i++)
  {
    if(tools.get(i).toString.compareTo("Loop") == 0)
    x.moveXPos(tools.get(i).)
  }
}
}
